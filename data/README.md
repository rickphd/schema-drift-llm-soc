# Released Data Artifacts

The public data release contains curated aggregate artifacts only. Raw telemetry logs, raw provider responses, repetition directories, local caches, and vector indexes are intentionally excluded.

Use `processed/reproduction_manifest.csv` as the configuration index for the released artifacts. Each listed `artifact_dir` points to aggregate CSV and summary files for one reported experimental condition.
